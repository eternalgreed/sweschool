/**
 * 6.1 Разберите свой код, и сделайте пять примеров, где можно более наглядно учесть в именах переменных уровни абстракции.
 * 1) hash - freeSlotIndex //индекс свободного слота в хэш-таблице
 * 2) hash - targetSlotIndex // //индекс слота с искомым значением в хэш-таблице
 * 3) count - currentFilledCount //заполенное количество элементов в буфере динамического массива
 * 4) current - removedElementFromTail // извлеченный элемент из хвоста очереди
 *    https://github.com/eternalgreed/sweschool/blob/main/algo1/src/main/java/Deque.java#L30
 * 5) elem - elementFromHead // элемент извлеченный из головы dequeue
 *    https://github.com/eternalgreed/sweschool/blob/main/algo1/src/main/java/Queue.java#L24
 *
 * 6.2 Приведите четыре примера, где вы в качестве имён переменных использовали или могли бы использовать технические термины из информатики.
 * 1) Очередь задач при реализации шаблона Producer-Consumer - taskQueue например
 * 2) Граф объектов в контейнере Spring Framework - objectsGraph - граф объектов
 * 3) Словарь соответствий серии и номера паспорта к конкретному человеку с ФИО - idToNameHashMap например
 * 4) Множество уникальных паролей - uniquePasswordSet
 * 5) Фабрика получения коннекшнов к базе данных - dataBaseConnectionFactory
 *
 * 6.3. Придумайте или найдите в своём коде три примера, когда имена переменных даны с учётом контекста (функции, метода, класса).
 * 1) isValidLogin - //результат проверки правильности логина
 * 2) highestRateItem - // предмет с самой большой оценкой - например в методе поиска предметов
 * 3) userRegion - допустим поле класса User , которое отвеччает за регион пользователя
 *
 * 6.4. Найдите пять имён переменных в своём коде, длины которых не укладываются в 8-20 символов, и исправьте, чтобы они укладывались в данный диапазон.
 * 1) mid - middleElementIndex
 * 2) i - movingIndex
 * 3) hits - countOfHits
 * 4) node - currentNode
 * 5) size - bufferSize
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 */


public class Variable_Names_2 {
}
